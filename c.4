%{
#include<stdio.h>
%}
%%
ifelse{printf("%s is a keyword", yytext);}
[0-9]+{printf("%s is a digit", yytext);}
[a-z A-Z]+{printf(%s is an an alphabet", yytext);}
.;
%%
int main()
{
printf("/n enter the input string:");
yylex();
}
int yywrap()
{
return 1;
}
